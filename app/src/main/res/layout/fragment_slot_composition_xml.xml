<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFF5F5F5">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimaryContainer"
            app:title="Slot API (XML)"
            app:titleTextColor="?attr/colorOnPrimaryContainer"
            app:navigationIconTint="?attr/colorOnPrimaryContainer" />

    </com.google.android.material.appbar.AppBarLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="0dp">

            <!-- Simple Info Card -->
            <include
                android:id="@+id/cardSimpleInfo"
                layout="@layout/view_slot_card" />

            <!-- Complex Action Card -->
            <include
                android:id="@+id/cardComplexAction"
                layout="@layout/view_slot_card" />

            <!-- Zebra Action Card -->
            <include
                android:id="@+id/cardZebraAction"
                layout="@layout/view_slot_card" />

            <!-- Zebra Animation Card -->
            <include
                android:id="@+id/cardZebraAnim"
                layout="@layout/view_slot_card" />

        </LinearLayout>
    </ScrollView>

</LinearLayout>
