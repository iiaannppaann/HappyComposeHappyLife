# Project-wide Gradle settings.
# For more details on how to configure your build, visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects.
org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages belong to AndroidX.
android.useAndroidX=true

# Automatically convert third-party libraries to use AndroidX dependencies.
android.enableJetifier=true

# Kotlin code style for standard formatting
kotlin.code.style=official
